# URL de base de l'API NestJS.
# En local : http://localhost:3000
# En production (Vercel) : définir cette variable dans
#   Project Settings → Environment Variables (avec le préfixe VITE_).
VITE_API_URL=http://localhost:3000
